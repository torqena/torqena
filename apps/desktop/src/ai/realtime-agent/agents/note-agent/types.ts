/**
 * Types for NoteManagementAgent
 */

import type { BaseVoiceAgentConfig } from "../../types";

/**
 * Configuration for NoteManagementAgent
 */
export interface NoteManagementAgentConfig extends BaseVoiceAgentConfig {
	// Note agent uses base config, no additional fields needed
}
